name: Update CarSwitch data (legacy - manual only)

# Disabled as an automatic job. The Playwright workflow is now the single
# scheduled updater so two scrapers cannot overwrite the same dashboard dataset.
on:
  workflow_dispatch:

jobs:
  legacy_notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain legacy workflow
        run: echo "Legacy updater is manual-only. Use update-carswitch-live.yml for scheduled Playwright updates."
