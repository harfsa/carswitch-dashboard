name: CarSwitch live data update

on:
  workflow_dispatch:
  schedule:
    # 05:00 Saudi Arabia (UTC+3) every day.
    - cron: "0 2 * * *"

permissions:
  contents: write

concurrency:
  group: car-switch-live-update
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Playwright
        run: |
          python -m pip install --upgrade pip
          pip install playwright
          python -m playwright install --with-deps chromium

      - name: Validate updater policy
        run: python scripts/update_carswitch_production.py --self-test

      - name: Live CarSwitch update
        env:
          DRY_RUN: 'false'
          PAGE_TIMEOUT_MS: '60000'
          WAIT_AFTER_LOAD_MS: '3500'
        run: python scripts/update_carswitch_production.py

      - name: Upload update report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: car-switch-live-update-report
          path: carswitch_update_report.json
          if-no-files-found: error

      - name: Commit dashboard dataset changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --quiet -- data/carswitch_data.json; then
            echo "No dashboard dataset changes to commit."
            exit 0
          fi
          git add data/carswitch_data.json
          git commit -m "Update CarSwitch live data"
          git push
